services:
  lrmu:
    build: .
    volumes:
      - ./logs:/tf/logs
      - ./plots:/tf/plots
      - ./tmp:/tf/tmp
      - ./:/tf/all
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 4
              capabilities: ["gpu"]
